# vercel:ai-sdk

Skill: build LLM features with the Vercel AI SDK.

## When to use
Streaming chat, tool calls, structured output, multi-provider routing in a Vercel app.

## Core idea
Provider-agnostic primitives (`generateText`, `streamText`, `tool`) keep model choices swappable. Pair with React hooks for UI streaming.
