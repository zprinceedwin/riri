# code-review

Skill: structured review of the current diff at configurable effort.

## When to use
Before merging; before declaring task complete; when an outside opinion is needed.

## Core idea
Adjustable depth (low → ultra). Surfaces correctness bugs, reuse opportunities, and simplifications. `--fix` applies findings; `--comment` posts them to a PR.
