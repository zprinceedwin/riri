# =============================================================================
# Riri - Voice AI Sales / Clinic Agents
# Copy this file to .env and fill in the values.
# Both apps/api and apps/web read from this single root .env via dotenv.
# =============================================================================

# -----------------------------------------------------------------------------
# AGORA (required) - https://console.agora.io
# -----------------------------------------------------------------------------
# From Agora Console > Project Management
AGORA_APP_ID=your_agora_app_id_here
AGORA_APP_CERTIFICATE=your_agora_app_certificate_here

# From Agora Console > Developer Toolkit > RESTful API
# These are base64-encoded as "Basic <base64(customer_id:customer_secret)>"
AGORA_CUSTOMER_ID=your_agora_customer_id_here
AGORA_CUSTOMER_SECRET=your_agora_customer_secret_here

# -----------------------------------------------------------------------------
# ANTHROPIC CLAUDE (required for LLM + summarizer) - https://console.anthropic.com
# -----------------------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-api03-your_anthropic_key_here
ANTHROPIC_LLM_MODEL=claude-sonnet-4-6
ANTHROPIC_SUMMARY_MODEL=claude-haiku-4-5

# -----------------------------------------------------------------------------
# VOYAGE AI (required for embeddings / RAG) - https://dash.voyageai.com
# Anthropic's official embeddings partner. voyage-3-large defaults to 1024-dim
# which MUST match the Supabase schema's vector(N) dimension.
# -----------------------------------------------------------------------------
VOYAGE_API_KEY=pa-your_voyage_key_here
VOYAGE_EMBEDDING_MODEL=voyage-3-large
VOYAGE_EMBEDDING_DIMENSIONS=1024

# -----------------------------------------------------------------------------
# ELEVENLABS (required for TTS) - https://elevenlabs.io
# -----------------------------------------------------------------------------
# Pick one voice id per persona from the ElevenLabs Voice Library.
ELEVENLABS_API_KEY=your_elevenlabs_key_here

# Jordan: fast-paced, charismatic, male - try "Brian" (nPczCjzI2devNBz1zQrb) or "Adam"
ELEVENLABS_VOICE_ID_JORDAN=nPczCjzI2devNBz1zQrb

# Mike: calm, authoritative, male - try "Daniel" (onwK4e9ZLuTAKqWW03F9) or "George"
ELEVENLABS_VOICE_ID_MIKE=onwK4e9ZLuTAKqWW03F9

# Sofia: warm, professional, female (clinic concierge).
# Suggested: Charlotte (XB0fDUnXU5powFXDhCwa) or Sarah (EXAVITQu4vr4xnSDxMaL).
ELEVENLABS_VOICE_ID_SOFIA=XB0fDUnXU5powFXDhCwa

# -----------------------------------------------------------------------------
# DEEPGRAM (required for ASR) - https://console.deepgram.com
# -----------------------------------------------------------------------------
DEEPGRAM_API_KEY=your_deepgram_key_here

# -----------------------------------------------------------------------------
# RESEND (required for booking confirmation emails) - https://resend.com
# Free tier (3,000 emails/month, 100/day) is plenty for the hackathon.
# RESEND_FROM_EMAIL must be a verified sender. The default uses Resend's shared
# onboarding@resend.dev which works without domain verification.
# -----------------------------------------------------------------------------
RESEND_API_KEY=re_your_resend_key_here
RESEND_FROM_EMAIL=Riri Clinic <onboarding@resend.dev>

# -----------------------------------------------------------------------------
# SUPABASE (required) - https://supabase.com
# -----------------------------------------------------------------------------
# Project URL: Supabase dashboard > Project Settings > API > Project URL
SUPABASE_URL=https://your_project_ref.supabase.co
# Service role key: Project Settings > API > service_role secret (NEVER expose to browser)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
# Embedding vector dimension. Schema is built for 1536; if you change your
# embedding provider to a different dim, re-run the schema migration.
SUPABASE_EMBEDDING_DIMENSIONS=1536

# -----------------------------------------------------------------------------
# CLINIC IDENTITY (V0 demo)
# -----------------------------------------------------------------------------
CLINIC_NAME=Belle Aesthetic Manila
CLINIC_DEMO_NAMESPACE=clinic-belle-manila
CLINIC_ADDRESS_LINE=Ground Floor, Bonifacio High Street, BGC, Taguig

# -----------------------------------------------------------------------------
# Riri APP (the LLM proxy URL Agora will call)
# -----------------------------------------------------------------------------
# Local dev: http://localhost:3001 (but Agora needs a PUBLIC url - use ngrok/cloudflared)
# Prod: https://your-railway-app.up.railway.app
API_BASE_URL=http://localhost:3001
# This is the URL Agora's Conversational AI engine will POST to as its llm.url
LLM_PROXY_URL=http://localhost:3001/v1/chat/completions

# Public URL of the web app (for CORS)
WEB_BASE_URL=http://localhost:3000

# API listen port
PORT=3001

# Next.js public-facing API URL (used by browser)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
