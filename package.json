{
  "name": "Riri",
  "version": "0.1.0",
  "private": true,
  "description": "Voice AI Sales Agents with Personality - Agora Hackathon Philippines 2026",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:api": "pnpm --filter @riri/api dev",
    "dev:web": "pnpm --filter @riri/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "seed:company": "pnpm --filter @riri/api seed:company",
    "seed:prospect": "pnpm --filter @riri/api seed:prospect",
    "seed:clinic": "pnpm --filter @riri/api seed:clinic",
    "seed:all": "pnpm seed:company && pnpm seed:prospect && pnpm seed:clinic"
  },
  "dependencies": {
    "hono": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "tsx": "^4.19.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.12.0"
}
