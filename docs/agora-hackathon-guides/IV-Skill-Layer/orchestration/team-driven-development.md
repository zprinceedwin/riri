# team-driven-development

Skill for coordinating multiple agents on a shared codebase.

## When to use
Multi-agent work where roles must hand off cleanly (PM → tech-lead → backend → frontend → QA).

## Core idea
Each agent owns its slice; the contract is the hand-off artifact. No agent reaches across boundaries silently.
